---
slug: huntflow-ux-agent-memory-enrichment
status: drafting
intent: unclear
review_required: true
plan_path: .omo/plans/huntflow-ux-agent-memory-enrichment.md
plan_sha256: null
review_round_id: null
pending-action: write and review .omo/plans/huntflow-ux-agent-memory-enrichment.md
review:
  momus:
    status: pending
    workspace_root: null
    runtime_home: null
    target: .omo/plans/huntflow-ux-agent-memory-enrichment.md
    round_id: null
    plan_sha256: null
    launch_id: null
    session: null
    result: null
  independent:
    status: pending
    workspace_root: null
    runtime_home: null
    target: .omo/plans/huntflow-ux-agent-memory-enrichment.md
    round_id: null
    plan_sha256: null
    launch_id: null
    session: null
    result: null
approach: Enrich Huntflow across 6 capability axes — richer on-screen info density, modernized user stories, deeper LLM/agent integration, agent-assisted LaTeX generation, shared enriched memory, and densified job-search/scraping — delivered as incremental, non-breaking enhancements to the existing Next.js 16 + SQLite + LangGraph + Scrapling stack
---

# Draft: huntflow-ux-agent-memory-enrichment

## Components (topology ledger)
<!-- Lock the SHAPE before depth. One row per top-level component that can succeed or fail independently. -->
| id | outcome | status | evidence path |
|---|---|---|---|
| C1 | Info-dense UI enrichment — every tracker/vault/resume/agent view surfaces 2-3x more evidence from DB without clutter | active | src/app/(app)/jobs/page.tsx, src/app/(app)/tracker/page.tsx, src/lib/db.ts:47-81 jobs schema, src/components/ui/* |
| C2 | User-story refresh — canonical stories rewritten around Discover→Rank→Analyze→Prepare→Apply→Track→Learn with acceptance in Gherkin | active | docs/PRODUCT.md, docs/INTELLIGENCE-PRINCIPLES.md, docs/ARCHITECTURE.md |
| C3 | LLM/agent diffusion — chat, match, and analysis LLMs moved from isolated /api/generate into tracker inline assists, vault Q&A, outreach drafting, and background suggestions | active | src/lib/llm/router.ts, src/agents/orchestrator.ts, src/app/api/generate/route.ts, src/app/api/assistant/route.ts |
| C4 | Agent-assisted LaTeX generation — resume LaTeX becomes agentic (draft→compile→audit→fix loop) with streamed diff, Synctex-aware preview, and ATS audit gate | active | src/lib/pdf/templates/*.tex, src/app/api/resume/compile/route.ts, src/agents/resumeAgent.ts, docs/RESUME-ENGINE.md |
| C5 | **Harness-grade memory (your prompt)** — Short memory per job offering (`memory.job_id`) + per job-search run (`threadId/runId`, TTL 7-30d, LangGraph pendingWrites) and Long memory (global summarized `memory` embeddings, accessible to all agents via `buildSharedContext` v2); optional Redis sidecar for pub/sub+cache, SQLite stays source of truth | active | src/lib/agents/memory.ts, src/lib/agents/context.ts, src/lib/db.ts:157-172+206-241 checkpoint tables, src/agents/multiAgentAppGraph.ts, src/lib/db.ts:742-834 |
| C5a | Context window & state management — token-budgeted window (`truncateHeadTail`, `budgetFor`, 8k sharedContext), sliding window, LLM compression/summarizer node, checkpoint continue/compress | active | src/lib/llm/tokens.ts, src/lib/llm/context.ts, src/lib/agents/checkpointer.ts, src/lib/agents/context.ts:111-113 truncateToTokens |
| C6 | Gap-closed job search & scraping — Scrapling sidecar hardening, source health observability, deduplication, and background enrichment (company intel + salary + fit) wired into discovery | active | scrapling-agent/server.py, scrapling-agent/sources.json, src/app/api/crawl/route.ts, src/app/api/agent/* |
| C7 | UI↔Backend wiring + bug-fix sweep — close every frontend-backend disconnect, fix silent failures, surface hidden fields, add missing endpoints | active | src/app/(app)/**/*, src/app/api/**/route.ts, src/context/AppContext.tsx, src/components/JobDetailDrawer.tsx |
| C8 | Security hardening — at-rest notes, import guard, embedding consent, path traversal, SSRF regression | active | src/lib/masking.ts, src/lib/db.ts, src/app/api/data/import/route.ts, src/app/api/agent/screenshot/[...path]/route.ts, docs/TRUST-BOUNDARIES.md |
| C9 | Test & documentation — green lint/typecheck/test/build, new deterministic tests, updated architecture docs with profile→vault→agent diagram | active | vitest.config.ts, vitest.setup.ts, src/lib/__tests__/vaultBm25.test.ts, docs/ARCHITECTURE.md, docs/RAG-AND-DOCUMENT-VAULT.md |

## Open assumptions (announced defaults)
<!-- Intent is UNCLEAR: research resolves ambiguity, defaults are adopted (not asked), and each is surfaced in the plan's human TL;DR for veto. -->
| assumption | adopted default | rationale | reversible? |
|---|---|---|---|
| LLM provider for new features | Reuse existing LLM router chain (openrouter default gemini-2.5-flash fallback) — no new provider signup required | Repo already has 11-provider router with circuit breaker; adding providers is scope creep | yes — swap model in settings.llm_providers |
| Vector embedding upgrade | Keep deterministic 256-dim local fallback as default, add pluggable OpenAI embeddings when key present; do not require hosted vector DB | Existing vault uses local BM25+hash embeddings, tests are deterministic; Pinecone/Qdrant would add infra | yes |
| Memory storage — short vs long (your harness Q) | **Short = SQLite memory rows scoped to `job_id` + `runId/threadId` (ephemeral, TTL 7-30d) + LangGraph checkpoint `agent_checkpoints/agent_checkpoint_writes` per thread; Long = consolidated SQLite `memory` (kind fact/decision/outcome) + optional `memory_embeddings` (256-d hash or OpenAI) summarized nightly via LLM and accessible to all agents via `buildSharedContext` — NO mandatory Redis for local-first; Redis offered as *optional* sidecar for pub/sub + cache when user opts into `docker --profile agent` | SQLite is already WAL+FK, backup single-tx `exportAllData`, and checkpoint-aware; Redis would add ops burden and break offline Docker `huntflow-data` volume portability — make it opt-in pub/sub layer, not source of truth | yes — flip `MEMORY_BACKEND=sqlite\|redis` in settings |
| Redis role | If enabled, Redis (ioredis) holds: (a) ephemeral run buffer `huntflow:run:{threadId}` with TTL 24h for streaming tokens/logs, (b) pub/sub `huntflow:memory:ingest` for cross-agent notify, (c) vector cache shadow of `memory_embeddings` — SQLite remains durable store, reconciled on `usageRepo.log` | Keeps local-first default zero-infra; power users get real-time harness without forking DB schema | yes — docker profile opt-in |
| Context window management | Hard budgets via `src/lib/llm/tokens.ts` `truncateHeadTail` + `budgetFor` per agent + sliding window + LLM summarization fallback; short memory feeds head, long memory feeds tail after compression | Already wired `truncateToTokens` binary search + `estimateTokens len/3.7`; adding summarizer node reuses `callLLM json:false` with 8k maxPrompt | yes |
| State management | Continue to use `SqliteCheckpointSaver` (`agent_checkpoints` + `writes`) + `agent_state` KV + `memoryRepo` + `agent_run_history`; continue via `resumeMultiAgentApp(threadId, Command{resume})` L541-563; compress via checkpoint pruning + summary checkpoint `put` after N steps | Already tested 600+ tests with temp DB per worker; no migration needed | yes |
| Agent framework | Stay on @langchain/langgraph 1.4.8 + SqliteCheckpointSaver, not switch to alternative orchestration | Already wired via checkpointer.ts + agent_checkpoints tables, no migration | yes |
| LaTeX toolchain | Keep local TeX Live + pdf-parse serverExternalPackages, agent loops via compile→log parse→LLM fix, not hosted LaTeX API | Docker already bakes texlive-*, standalone tracing includes templates | yes |
| Scraping | Harden existing Scrapling + Playwright sidecar, do not add third-party job API aggregation yet | sources.json is source of truth; sidecar already has X-Huntflow-Token auth | yes |
| UI density | Progressive disclosure (collapsible evidence panels, inline badges, drawer enrichment) over full redesign | Preserves Tailwind 4 token system src/lib/theme.ts and cn() conventions | yes |
| Analytics for new info | Compute derived stats in src/lib/db.ts:computeStats and surface in UI, no new BI warehouse | Stats already computed for dashboard | yes |

## Findings (cited - path:lines)
- DB health: DatabaseSync WAL+FK+busy_timeout 5000 at src/lib/db.ts:28-30, 17 tables, jobs 32 cols including JSON payloads salary_intel/multi_agent_outputs/employer_review/star_flashcards at L47-81, ALL_TABLES_IN_DELETION_ORDER 17 entries L1264-1282, backup single transaction L1380-1444
- LLM router: 11 providers in src/lib/llm/providers.ts:17-132, resolveChain merges DB→env→legacy L67-118 in router.ts, eligible filters + circuit MAX_FAILURES=3 COOLDOWN 90s L32-34, budgetFor 25 budgets in context.ts:6-29 (pitch 4k/500, match 10k/2k)
- Agents: Orchestrator MAX_ITERATIONS=3 src/agents/orchestrator.ts:41, 3 nodes route/executeTool/compose L399-409; ApplyAgent 5 nodes src/agents/applyAgent.ts:192-205; MultiAgent 11 nodes with fan-out/in + HITL interrupt src/agents/multiAgentAppGraph.ts:79-427, DEFAULT_RETRY_POLICY 3 attempts L378-382, SqliteCheckpointSaver src/lib/agents/checkpointer.ts
- Memory: memory table kind note|insight|fact|decision|outcome L157-165, memoryRepo significant at src/lib/db.ts:742-800, wrappers remember/rememberUnique/relevantMemory 40 limit scoring in src/lib/agents/memory.ts:55-80, buildSharedContext 8k tokens joins profile+pipeline+memory+usage L45-124 in context.ts
- Tools: 9 Zod schemas + handlers in src/lib/agents/tools/multiAgentTools.ts:14-402 (companyIntel via Wikidata/Wikipedia/GDELT, salaryIntel regional fallback, PII sanitizer regex)
- API surface: ~55 handlers enumerated (hydration, LLM, vault, resume, agent/scraping, mail, linkedin) — no auth middleware, relies on 127.0.0.1 binding in next.config.ts:7 + docker-compose
- Frontend: Jobs DiscoveryControl at src/app/(app)/jobs/page.tsx 599 lines with source-select grid, crawl telemetry, deck/matrix views, dedupKey, decisions capped 500; 12 pages under (app) including tracker/vault/resume/agent/assistant; design tokens in src/lib/theme.ts/globals.css, ui primitives cn()+forwardRef+lucide
- Vault RAG: 5 files in src/lib/vault (extract→chunk 700/90→BM25+256-dim hash→RRF), vaultRepo chunksFor/allChunks/distinctEmbedModels L882-983, API vault search cosine 0.12 threshold with keyword fallback
- Scrapling: FastAPI server.py require_token on every endpoint if HUNTFLOW_AGENT_TOKEN set, sources.json category static/stealth/posts, crawler concurrency default 4
- Resume engine: LaTeX templates in src/lib/pdf/templates/*.tex traced via next.config.ts:11-13, serverExternalPackages pdf-parse, multiple compile/render/ats/synctex routes, resumeAgent tasks draft/improve/tailor/ats/parse_pdf with deterministic fallbacks
- **PROFILE VAULT deep dive (your follow-up)**: Profile is NOT a table — it's JSON blob `settings.profile` via `settingsRepo.get/set` + `initialProfile` (seed at db.ts:1325), edited in `src/app/(app)/vault/page.tsx:88-865` tab "info" with 30+ fields (name/email/phone/address/city/country/linkedin/github/portfolio/workPermit/salary/notice/workMode/relocate/years/DOB/nationality/visa/gender/veteran/disability/clearance/license/languages/marital etc) validated by `VaultProfileSchema` + `formatZodErrors` L112-126, saved via `useApp().updateProfile` → `POST /api/data/settings` (AppContext). Vault docs are separate `vault_docs`/`vault_chunks` with labels `resume|cv|uni_marks|uni_paper|attestation|image` auto-assigned by `autoLabel` L71-80, stats bytes/chunks/docs, 25MB upload cap `api/vault/route.ts:5`, RRF hits expose doc/chunk/model/ranks/terms/strategy. **Gaps**: (1) no chunk inspector — `vaultRepo.chunksFor(docId)` exists but UI never calls it, (2) no profile↔vault link — `resumeAgent parse_pdf` and `multiAgent resumeCVTailor` take profile but vault chunks are never injected as evidence, (3) profile form has silent 400ms fake-save (no API error handling for offline), (4) no sync indicator that profile is used by agents on-the-go
- **How agents pull data on-the-go**: Every agent invocation rebuilds fresh context: `buildSharedContext({profile,jobs,emails,interviews,reminders,memoryLimit,maxTokens})` at `context.ts:45-124` queries `memoryRepo` via `relevantMemory(query=targetTitle+skills+open job titles, limit 40)` + `jobsRepo.list()` + `usageRepo.totals()`, truncates to 8k tokens. Orchestrator `runAssistant` builds 5k sharedContext and injects into LLM route prompt L60-155; `apply-agent/route.ts:28-35` builds 6k sharedContext as `pitch` context; `multiAgentAppGraph companyIntelNode L101` writes verified facts via `rememberUnique("fact",...,jobId,importance:2)` which next `buildSharedContext` call surfaces via jobId scoring. **Gaps**: (a) profile snapshot is captured at graph start and never refreshed mid-run (11-node pipeline can run minutes), (b) vault RAG is NOT in sharedContext — agents only get `search_jobs`/`search_vault` as on-demand tools, not proactive context, (c) no streaming memory subscription — agents poll via next build, not event-driven, (d) HITL `interrupt` at `autoApplyExecutionNode L302-349` loses sharedContext freshness on resume via `Command`
- **UI ↔ Backend connection gaps / bugs**: (1) `JobDetailDrawer` and `tracker/page.tsx` never surface 8 hidden job cols (employerReview/salaryIntel/skillsGap/jobBrief/etc) — data fetched but not rendered; (2) `vault/page.tsx` search shows hits but not chunk viewer — `GET /api/vault/search` returns hits with chunkIndex but `vaultRepo.chunksFor` detail endpoint missing; (3) `resume/page.tsx` compile flow has no live log streaming — `POST /api/resume/compile` returns binary PDF or 500 but LaTeX log is discarded; (4) `assistant/page.tsx` streams `runAssistantStream` SSE but has no cancellation on tab switch — `AbortController` not wired; (5) `agent/page.tsx` polling `agent/activity` every 3s but `runId` mismatch causes dropped telemetry; (6) offline handling in `jobs/page.tsx` sets `offline=true` but retry only pings `/api/agent/health` not vault/llm health; (7) `AppContext` hydrates via `GET /api/data` bootstrapSeed but never revalidates after `POST /api/data/import` until reload
- **Security (quick audit)**: `maskSecret`/`redactSettings` at `masking.ts:5-66` + `gmailAuth.ts:158` collapse `gmail_oauth` to connected/email/expiry — good. Sidecar `require_token` at `server.py:215` checks `X-Huntflow-Token` when `HUNTFLOW_AGENT_TOKEN` set else open — good for local. SSRF guard `assertPublicUrl` at `scrape/route.ts:12-60` blocks localhost/10/127/169.254/172.16/192.168/224 + IPv6 ::1/fe80 — good, tested for literal detection `_looks_literal` at `server.py:484`. **Issues**: (a) secrets stored plaintext in SQLite `settings.value` — `TRUST-BOUNDARIES.md` flags as roadmap, no at-rest encryption; (b) no rate-limit or auth on any `/api/data/*` or `/api/generate` — relies solely on `allowedDevOrigins ["127.0.0.1"]` + docker `127.0.0.1:3000` binding; (c) `POST /api/data/import` restores masked secrets via `restoreMaskedSecrets` but does not verify `app=="huntflow"` signature beyond string compare; (d) `vault/embedding fallback` sends doc text to external provider only when `resolveChain` finds key — disclosure is opt-in but not per-doc consent UI; (e) `agent/screenshot/[...path]` 5s proxy has no path traversal guard beyond no-follow open in scaffold — needs audit
- **Tests / docs**: `vitest.config.ts: forks pool, 60s timeout, HUNTFLOW_DB_PATH temp per worker in vitest.setup.ts` — >600 tests across unit/API/persistence/workflow, deterministic BM25 tests at `vaultBm25.test.ts` must not be mocked. CI `ci.yml: secret-scan → lint → typecheck → test → build`. Docs at `docs/PRODUCT.md|INTELLIGENCE-PRINCIPLES.md|RAG-AND-DOCUMENT-VAULT.md|RESUME-ENGINE.md|ARCHITECTURE.md` are current but lack profile-vault cross-reference and agent memory flow diagram

## Decisions (with rationale)
- Treat as UNCLEAR + Architecture tier → adopt defensible defaults, auto high-accuracy dual review after plan completion (per intent-unclear.md)
- **Nine-component topology (C1-C5a-C9) expanded per your harness prompt + live + RAG agentic asks** mirrors user's explicit role map: data scientist→C5 vault/memory analytics + RAG eval, AI engineer→C3/C5/C5a (agents + window + compression + 11-node agentic), UI/UX→C1/C2 + C1-LIVE streaming, backend→C5/C5a/C6 + DB, automation→C4/C6, scraping→C6, security→C8, QA/docs→C9 — each component independently succeeds/fails
- **Harness foundation checklist (areas not to neglect — your ask)**: (1) orchestration (LangGraph graphs + HITL + retry), (2) tool registry (9 Zod tools at `tools/multiAgentTools.ts`), (3) state (checkpoint + agent_state + threadId), (4) memory (short/long + embeddings + relevance), (5) context window (budgets + truncate + summarizer), (6) observability (`usage_log`, `agent_run_history`, `notifications`, SSE streaming), (7) evaluation (BM25 determinism, `computeStats` analytics), (8) error handling (circuit breaker, offline health), (9) security (masking, SSRF, token), (10) persistence (single-tx backup/restore + migrations idempotent `addColumn`)
- **RAG insanely well engineered → adopted default**: keep `700/90` chunk + `k1 1.2/b 0.75` BM25 as baseline, add LLM query expansion + hybrid + reranker (local cross-encoder or `rerank` API) + citation viewer + eval harness (`Recall@50/nDCG@10`) — no hosted vector DB required, but `openai|text-embedding-3-small` when key present; all mixing guarded at `index.ts:102-107`
- **11 nodes legit agentic → adopted default**: convert 7 deterministic nodes to `callLLM JSON + internet search tool (sidecar /scrape as proxy) + reasoning loop` with `DEFAULT_RETRY_POLICY 3×1s×2` preserved, keep PII/regional rules as validator, not generator
- **Live crawlers → adopted default**: SSE over polling, per-board `sourceResults` already emitted at `server.py:835` surfaced as streaming cards, screenshots from `server.py:181`
- No mandatory Redis: keep SQLite durable as `local → optional Redis sidecar` to avoid breaking local-first Docker volume `huntflow-data` portability; Redis only for pub/sub + ephemeral cache when user opts in
- Plan must not invent reduced MVP; full scope per full-workflow.md universal invariants
- **What else you're missing — agents wise (your ask, folded as Scope IN companion notes, not new components)**: supervisor/orchestrator-v2 (already exists as `orchestrator.ts` but needs wiring to 11-node), evaluator/critic agent (scores ATS + factuality), memory-consolidator (long-memory summarizer), scraping specialist agent (owns sidecar health + selector healing), resume LaTeX critic (loops until `latexmk` log clean), UX copilot (reads UI telemetry and suggests story gaps) — see brief below for full list and where they plug in; all are covered as expanded todos inside C3/C5/C4, not new topology rows

## Scope IN
- C1: Enrich tracker/job cards/drawer with hidden DB fields (employer_review, salary_intel disclosed vs estimated, skills_gap, job_brief, fitCategory badges, screenshot proof, auto_apply_logs timeline, multi-agent outputs)
- C1-LIVE (your latest): **Job-find crawlers LIVE UI** — replace 3s poll (`agent/activity`) with SSE streaming `GET /api/crawl/stream` + `GET /api/agent/activity?since` already at `server.py:1073`; live board cards with found/matched/error per `CrawlSourceResult`, runId timeline, concurrency workers gauge, screenshot proof thumbnails (from `server.py:181 shot()`), interrupt/pause controls; fix dropped telemetry when `runId` mismatches
- C1: Vault doc detail enrichment (chunk table via new GET /api/vault/chunks?docId, RRF scores, embedding model label, matched terms) + fix missing chunk inspector bug
- C1: Resume studio enrichment (template typography, live compile log streaming, ATS score breakdown)
- C1.1 (from your follow-up): Profile vault hardening — wire profile↔vault evidence (inject vault hits into resume tailoring), fix fake-save error handling, add sync indicator that profile is agent source-of-truth, expose autoLabel editing
- C2: Rewrite user stories (docs/PRODUCT.md + docs/INTELLIGENCE-PRINCIPLES.md flow) with Gherkin acceptance, map to pages/components, document profile→vault→agent data flow
- C3: Wire LLM assists into tracker (inline explain fit), vault (Q&A with citations via searchVault k=4 threshold 0.12), outreach (agent-drafted with memory of prior outreach), assistant streaming everywhere + fix streaming cancellation (AbortController)
- C3.1: Agents pull data on-the-go fix — refresh profile mid-run (reload from settingsRepo before each node), inject vault RAG proactively into sharedContext, preserve context freshness across HITL resume via Command, add event-driven memory subscription (polling buildSharedContext v2)
- C3-AGENTIC (your latest): **Make 9/11 nodes legit agentic (not regex)** — today only `companyIntel` (internet via Wikidata/Wikipedia/GDELT in `companyResearch.ts`) + `salaryIntel` (LLM prompt + regional deterministic fallback) are agentic; remaining 7 (`regionalNorms`, `piiSanitizer`, `resumeCVTailor`, `letterTailor`, `interviewPrep`, `outreachEmail`, `atsAudit`) are deterministic regex at `tools/multiAgentTools.ts:150-402`. Replace each with LLM `callLLM json:true` + `internet search` tool (Tavily/Serper via sidecar `POST /scrape` as search proxy) + structured reasoning (`chain-of-thought` JSON hints `sanitize.ts:clean*`) + tool calling loop (`MAX_ITERATIONS 3` pattern from `orchestrator.ts:41-62`); keep regional PII rules as guardrail, not sole logic
- C5-RAG (your latest): **RAG insanely well engineered** — today `extract→chunk 700/90→local 256-d hash→BM25 k1 1.2 b0.75→cosine 0.12→RRF k60` at `vault/*` is minimal; upgrade to: query expansion (LLM rewrites) + hybrid (BM25 + vector) + cross-encoder reranker (local or `openai` rerank) + citation chunk viewer with score breakdown + embedding model coherency (block `openai|…` vs `local` mixing at `index.ts:102-107`) + eval set (`Recall@50/nDCG@10/MRR` from `docs/INTELLIGENCE-PRINCIPLES.md` roadmap) + ingest visibility (per-doc `status: indexing→ready|failed` + chunk count + error)
- C4: LaTeX agent loop (LLM drafts .tex → compile → parse log → LLM patches → ATS audit → human approve) with diff preview and Synctex, preserve LaTeX log on failure
- C5: **Harness memory — short vs long (your prompt)**: Short = per-job-offering (`memory.job_id` + importance) + per-job-search-run (`threadId`/`runId` ephemerals in `agent_checkpoints`/`writes` + `memory source=search-run` TTL 24h-7d) for working context; Long = global consolidated memory (kinds `fact|decision|outcome`, nightly LLM summarizer consolidates job-scoped episodic into deduplicated insights with `memory_embeddings` cosine 0.12 threshold, TTL 90d+ with decay, accessible to all agents via `buildSharedContext v2` relevance ranking `jobScore 40+overlap*4+importance*6` at `memory.ts:70-74`); optional Redis sidecar for pub/sub `huntflow:memory:ingest` + run buffer cache, SQLite remains durable source of truth
- C5a: **Context window & state (harness foundation you asked not to neglect)**: Window mgmt via `budgetFor` per agent (`context.ts:6-29`) + `truncateHeadTail`/`truncateToTokens` binary search + `buildSharedContext` 8k truncation L112 + sliding window over `jobs.slice(-6)` + LLM compression node (summarize tail when `estimateTokens>maxTokens`); State mgmt via `SqliteCheckpointSaver.getTuple/list/put/putWrites` (`checkpointer.ts`) + `agent_state` KV + `agent_run_history` + `memoryRepo`; Continue from agent/run via `resumeMultiAgentApp(threadId, Command{resume})` at `multiAgentAppGraph.ts:541-563` + `autoApplyExecutionNode interrupt` L302-349; Compress memory via checkpoint pruning + summarizer checkpoint after N steps + `memoryRepo.prune(500)` extended to TTL-aware + embedding dedup
- C6: Scraping — source health dashboard, retry/backoff visibility, dedup + skip_reason surfacing, background enrichment queue (company/salary/fit) on crawl results + fix activity runId dropped telemetry
- C7 (NEW — your follow-up): UI↔Backend wiring + bug sweep — audit all 55 /api routes vs frontend call sites, add missing detail endpoints, fix offline/retry paths, hydrate after import, wire missing error toasts
- C8 (NEW — your follow-up): Security audit — at-rest secret notes, rate-limit discussion for /api/data, import signature hardening, per-doc embedding consent UI, screenshot path traversal guard, SSRF guard regression test, document trust boundary updates
- C9 (NEW — your follow-up): Test & documentation wave — run `npm run lint && npx tsc --noEmit && npm test && npm run build`, fix any regression, add deterministic tests for memory ranking + vault hybrid, update docs/ARCHITECTURE.md and docs/RAG-AND-DOCUMENT-VAULT.md with profile-vault-agent diagram
- Cross: masking/redaction preserved (maskSecret, redactSettings), backup/restore includes new memory embeddings, tests for BM25/RAG/memory ranking determinism

## Scope OUT (Must NOT have)
- No hosted multi-tenant auth / RBAC — remains single-user local-first (per AGENTS.md + TRUST-BOUNDARIES.md)
- No Pinecone/Qdrant/hosted vector DB replacement — SQLite remains source of truth
- No switching agent framework (stay LangGraph)
- No public internet exposure or binding change from 127.0.0.1
- No new LLM provider billing — reuse existing chain
- No migration to pages router or server actions — REST /api/* stays
- No encrypted-at-rest secrets change in this tranche (roadmap item) — continue masking flow
- No guarantee of ATS outcome — templates stay conventional, audited not certified

## Open questions
- None blocking plan; defaults above cover forks. One owner-decision flagged for gate: confirm memory retention default (episodic 90d TTL vs indefinite) — plan defaults to 90d with prune(500) extended to TTL-aware, user can veto to indefinite.

## Approval gate
status: awaiting-approval
approach: Nine-component harness across 6 waves + RAG-live-agentic cross-cuts; Wave 1 memory harness (short/long + window/state) + RAG insane upgrade (expansion+hybrid+rerank+eval) + profile-vault linkage, Wave 2 UI enrichment + LIVE crawler streaming (SSE + board cards + screenshots) + wiring/bug fixes, Wave 3 LLM diffusion + 11-node agentic upgrade (7 nodes to internet-tool reasoning) + on-the-go refresh, Wave 4 LaTeX agentic loop, Wave 5 scraping hardening + enrichment queue, Wave 6 stories/evaluator/critic/docs/security/tests. Each todo carries exhaustive References + agent-executable Acceptance + happy/failure QA + Commit. Updated per your latest to cover RAG perfection, live crawlers, legit agentic nodes, and harness gaps.
pending-action: write and review .omo/plans/huntflow-ux-agent-memory-enrichment.md
